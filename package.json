{
  "name": "velox2",
  "version": "1.0.0",
  "description": "Velox chat app",
  "main": "run.js",
  "scripts": {
    "start": "electron .",
    "package": "electron-packager . Velox --platform=win32 --arch=x64 --overwrite --icon=icon.ico",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Your Name",
  "license": "MIT",
  "devDependencies": {
    "electron": "^25.9.8",
    "electron-packager": "^17.1.2"
  },
  "dependencies": {
    "electron-updater": "^6.6.2"
  }
}
